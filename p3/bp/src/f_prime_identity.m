% Function: Derivative of the identity function.
%
% Description:
%   Evaluates the derivative of the identity function.
%
% Parameters:
%   f_y: value of f(y) where f is the identity function.
%
% Return:
%     f'(y) where f is the identity function.
%
function f_prime = f_prime_identity(f_y)
  f_prime = ones(size(f_y));
end